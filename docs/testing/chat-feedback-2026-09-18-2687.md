# Chat feedback repairs — September 18, 2026

Reported installation: both phones use 2.4.8 (2026072687). Encrypted
placeholders affect new and old messages. The feedback phones are not the
locally connected devices.

Chat source: `16074f4a57be5b3452d807a2bbcf30fc23082d15`.

| Feedback | Repair and verification |
| --- | --- |
| Account created, then username-used error | Serialize normal/anonymous registration submissions and guard keyboard submission while loading. Duplicate-request regression passes; the original dxx10 server incident is not independently established. |
| Encrypted placeholders after login | Notify existing timelines after restoring device-local session keys. Coalesce encrypted-send preparation and retry an incomplete device/Olm refresh once. Preserve trust checks and encryption. Native secure-storage acceptance remains open. |
| Groups list shows live rooms and a function | Exclude live directory topics and call the localized count function. |
| Group avatar missing and count/name incorrect | Include avatar in initial room state, preserve navigation metadata, hydrate membership, and count known joined/invited members. Invitation is not acceptance; a newly created three-person group can contain one joined and two invited users. |
| Tag contact count remains zero | Load friend annotations and derive counts from assignments. Scope catalogs by server/account and propagate tag rename/delete to annotations. |
| Contact recommendation fails | Apply coalesced/retried encrypted readiness; render localized failures. Live production contact-card delivery/decryption passes. |
| Star disappears on reopening | Persist the star per server/account/friend, reload it, and render its contact-list indicator. Widget persistence test passes. |
| Video feed spins forever | Resolve Matrix media using authenticated download, play a disposable local file, and display failure/retry instead of endless loading. Download tests cover HTTP errors, cleanup, and credential-safe redirects; feedback-device playback remains unverified. |
| Video author avatar/profile absent | Use the authenticated avatar component and route avatar/name taps to the user's profile. |
| Favorites visible across accounts | Scope records, caches and in-flight operations by server/user. Test cross-account/server access, restart and account-switch races. Retain old unowned global records without exposing or automatically assigning them to an account. |

## Evidence and limits

- Chat: 153 focused tests plus one tag-count widget test pass. Analysis has
  no errors/warnings (235 informational diagnostics).
- Live SDK test: two-way encrypted text, encrypted contact card, group avatar
  plus creator/two invitees, and old/new message decryption after actual
  SDK logout/login all pass. Uses real Matrix server, native crypto and
  separate SQLite databases, with **mocked secure storage**. All three
  temporary accounts were deactivated; no feedback-user data was changed.
- App: all 923 Chat regression tests pass; analysis has no errors/warnings
  (217 informational diagnostics). The gallery fixture now waits for actual
  persisted imports instead of assuming disk work finishes within 200 ms.
- All 781 mirrored Chat `lib`/`assets` files match the app's resolved Git
  dependency. App tests exercise Matrix SDK 6.2.0, while the standalone Chat
  worktree uses 6.1.1.
- Already-lost historical keys without a retained copy cannot be recreated.
  These results do not establish that dxx/dxx01's native logout persistence
  or video playback is fixed on their phones. Keep that acceptance open in
  Chat's `OPEN_ISSUES.md` (QA-009).

## Colleague's iOS build failure

The release Mac disabled Flutter SwiftPM globally; the project did not record
that choice. Commit `cf42dba72` makes plugin resolution project-local and adds
[reproduction instructions](../IOS_DEPENDENCY_TROUBLESHOOTING.md).
Locked WebRTC still supports CocoaPods and resolves alongside ML Kit.
Both iPhone Debug and Release compile without signing. The colleague confirms
the same Archive/Debug error but has not supplied its first error yet;
their precise failure remains unconfirmed.
