# 回归截图 1、2、3.png 对照记录

> 后续状态：服务端普通注册已恢复并通过临时账号验证；连接 Android 的完整应用已重新安装。TURN 前置网关故障仍待处理。详见 [服务端验证记录](../matrix-server-validation-2026-09-16.md)。

## 来源与结论

来源：用户下载目录的 `1.png`、`2.png`、`3.png`，共 25 个编号。用户已确认截图使用 2026072670 或更早版本。最近已发布版本为 2.4.8（2026072671），不能将旧包截图视为 2671 修复失败的证据；本轮新增修复也尚未包含在 2671 中。

- 明确回归通过：6 项（6、12、13、15、16、25）。
- 标记待修复，但明确注明“好友列表清空，且加好友失败，暂无测试数据”：12 项（2、3、4、5、9、10、11、14、17、18、20、21），归类为被阻塞，不能作为各功能已再次复现失败的证据。
- 其他明确待修复：6 项（1、8、19、22、23、24）。
- 第 7 项的状态栏没有独立结论，不推定通过。

## 逐项对照

| 编号 | 问题 | 截图结论 | 本轮处置／下一步 |
| --- | --- | --- | --- |
| 1 | 注册被禁用 | 待修复，需服务端 | 已用用户确认的 ubuntu 登录；备份后启用普通用户名密码注册并热加载。两个临时账号完成真实注册、登录和好友邀请验证，随后停用；见服务端验证记录。 |
| 2 | 恢复后消息仍显示加密 | 无测试数据，阻塞 | 保留新消息密钥投递门禁和恢复数量提示；待好友链路恢复后验证新消息。没有备份密钥的历史消息不承诺恢复，不能关闭加密来隐藏问题。 |
| 3 | 加载图标应在消息前 | 无测试数据，阻塞 | 已将发送中图标移到自己消息气泡前；测试同时覆盖发送中、已发送及收到的消息。 |
| 4 | Android／iOS 两账号音视频不通 | 无测试数据，阻塞 | 已定位当前下发 TURN 地址返回 HTTP 400；前置网关 SSH 授权待补充。真实双机连接仍待验证。 |
| 5 | 系统／应用通话界面、隐藏后无法返回、挂断残留 | 无测试数据，阻塞 | iOS 系统通话界面来自 CallKit 集成；系统与应用界面需要同步状态。既有返回入口及挂断修复保留，待双端／后台／锁屏验收。 |
| 6 | 文件下载操作退出聊天后仍显示 | 已通过 | 保留通过结论。 |
| 7 | 扫码后一方已好友、另一方仍待同意 | 无独立状态 | 继续要求双方加入后才算好友及允许发送；与第 8 项共同验收。 |
| 8 | 重复扫码产生多个聊天室，无申请且发送失败 | 待修复 | 增加同一好友请求合并、不同好友建房串行化；SDK 已确认写入 `m.direct` 后立即反映到本地缓存，避免同步窗口重复建房。成员查询单点失败不再阻断其他好友；所有可用联系人都无法确认时保留错误而不返回伪空列表。申请读取与联系人查询失败解耦，联系人监听在后续同步可恢复。未删除历史房间或伪造好友同意。实际账号恢复仍待验证。 |
| 9 | 点联系人应直达资料页 | 无测试数据，阻塞 | 既有跳转测试通过；新增刷新／错误／启动聊天时保留已加载联系人显示。 |
| 10 | 好友详情回显电话、标签、备忘、照片 | 无测试数据，阻塞 | 保留已有实现和回归测试，待恢复联系人后真机验收。 |
| 11 | 修改备注页应只显示备注字段 | 无测试数据，阻塞 | 既有单字段编辑测试通过，待真机复核。 |
| 12 | 电话无法输入 | 已通过 | 保留通过结论。 |
| 13 | 标签保存无效；另询问能否多选 | 已通过 | 保留通过结论；增加同时选中两个标签并保存的回归，现有实现支持多选。 |
| 14 | 标签无新增入口 | 无测试数据，阻塞 | 保留已有创建入口、选中新标签和名称去重测试。 |
| 15 | 备忘保存不回显 | 已通过 | 保留通过结论。 |
| 16 | 照片错误进入朋友圈 | 已通过 | 保留通过结论。 |
| 17 | 照片只能单选 | 无测试数据，阻塞 | 既有多选、预览、删除、失败清理测试通过，原生相册多选仍待反馈设备验收。 |
| 18 | 朋友权限未影响朋友圈 | 无测试数据，阻塞 | 保留 2671 的成员权限与独立受限房间修复；本轮补充从朋友圈／视频发布入口传递联系人状态，防止受限名单选择页为空。区分“不让他看我的”和“不看他的”；前者限制对方访问，后者过滤自己看到的内容。待双账号按方向验收。 |
| 19 | 好友朋友圈头像不能进资料页 | 待修复 | 封面好友头像、动态作者头像已绑定资料页，保留联系人 Bloc 状态。 |
| 20 | 好友视频号无响应 | 无测试数据，阻塞 | 代码确认仍为不可点击占位区。改为真实入口，按好友 ID 读取其视频，并复用已有权限过滤；移除伪视频缩略图。 |
| 21 | 好友资料音视频按钮无响应 | 无测试数据，阻塞 | 既有入口及失败反馈测试保留，待实际通话验证。 |
| 22 | 发布定位仅展示经纬度 | 待修复 | 增加反向地址解析并保存可读地址；无法联网／地址服务不可用时仍保留真实坐标，不编造位置。 |
| 23 | 动态详情作者头像不能进资料页 | 待修复 | 已绑定同一资料页入口，组件测试验证用户 ID 正确。 |
| 24 | 动态删除后仍出现 | 待修复 | 在已有按实际房间删除的基础上，只有服务器确认撤回后才加入当前会话删除记录；旧时间线、旧订阅和迟到加载结果不能重新显示。未找到事件／撤回失败不会伪报成功。 |
| 25 | 视频号误进频道目录 | 已通过 | 保留通过结论，继续使用视频发布／直播入口。 |

## 本轮验证

- Chat 专项回归：131 项通过，覆盖联系人／申请、朋友圈权限／删除、Bloc、头像跳转、好友视频入口、定位、消息状态以及资料／照片／多标签。
- 重复扫码用例使用真实 Matrix SDK `Room`，在服务器房间已到达而 `m.direct` 尚未到达时验证第二次操作复用同一房间；网络请求仍用模拟对象。
- 静态分析：无 error／warning；主项目集成结果另记录在钱包仓库对应报告。

## 验收边界

这些是代码修复和自动化回归，不是 dxx／dxx01 真实账号的通过结论。当前连接设备与用户反馈设备不同；客户端阶段没有操作其账号、没有创建生产测试好友、没有发起生产通话或修改注册配置。后续服务端阶段使用临时账号验证并清理，现已修复注册，见服务端验证记录。安装包版本已确认；部署 SSH 登录已成功，通话前置网关授权仍待补充。已完成可独立推进的客户端修复。

朋友照片及附加资料仍为按服务器／账号／好友隔离的设备本地数据，没有新增云端跨设备同步。历史重复房间未自动删除；客户端本轮也未移除任何 `m.direct` 项或已有聊天记录。历史密钥缺失、已下载社交内容的撤回限制延续前次审计结论。后续验收应先确认两台设备版本，再做请求／接受／拒绝、新消息、音视频、资料与权限链路。

## 后续反馈与补充修复

- 用户确认截图来自 2026072670 或更早版本；最新文字反馈保留注册、朋友圈列表／详情头像、好友视频号和位置名称问题，并新增 Android 扫码权限页闪屏。用户表示其他昨日问题已通过，但明确列出的消息／通话／联系人／朋友权限仍因好友链路阻塞而未验证；上表保留原始截图状态供对照。
- 扫码：修复前台恢复时重复申请权限、反复销毁控制器的问题。现在申请串行执行，前台恢复只检查状态，保留原控制器；后台暂停／前台恢复串行执行。拒绝权限页可滚动，窄屏按钮可换行。4 项组件用例覆盖拒绝、弹窗触发生命周期、检查期间仍可操作以及相机恢复；尚未证明反馈手机的原生权限弹窗已通过。
- 好友：联系人与申请读取分开处理，任一失败保留对应已有列表；联系人订阅异常仍刷新申请。有申请时，联系人错误页不再遮住“新的朋友”入口及申请列表。接受后即时保留服务器已确认的私聊映射，避免等待 account-data 同步期间好友消失；不存在的邀请不再返回假成功。
- 补充专项测试：96 项通过，包括真实 SDK Room 的收件邀请、接受后同步延迟、部分加载失败和入口可达性。网络仍为模拟，不能代替两个实际账号验收。

- 主项目 Git 依赖集成：398 项反馈及钱包桥接测试通过；静态分析 0 error／0 warning。组合执行暴露备注服务缓存跨测试残留，已在资料页测试初始化中按模拟存储刷新，确保用例隔离。主项目真机相机补测结果见钱包仓库同日回归报告。

## 主项目与真机验证结果

- 主项目反馈回归与钱包桥接：398 项通过；`flutter analyze --no-fatal-infos`：0 error、0 warning（196 项 info）。
- Chat 实际解析来源为 Git，779 个 lib/assets 文件与缓存镜像逐项 SHA-256 一致；固定引用见 pubspec.yaml 和审计清单。
- 当前连接的 Android（25098RA98C）执行 `integration_test/chat_scanner_camera_device_test.dart`：1 项通过。使用真实相机，验证启动、3 次暂停／恢复、控制器保留以及退出释放；未使用任何 Matrix 账号或提交扫码内容。原生权限由 ADB 授予，因此不将此结果视为权限弹窗手动点击已通过；生命周期由测试触发，不替代真实系统切换、锁屏等场景。
- 真机使用本轮代码构建的开发检查包，测试时版本字段仍为 2026072671；它不是此前交付的 2671 发布包。此前发布的 APK／TestFlight 均不含本轮修改。恢复完整应用时安装两次被系统拒绝，详见下述设备操作记录；反馈的两台设备仍需安装后续发布包复测。
- 服务端注册现已修复；临时账号之间的申请／同意／拒绝已验证，用户设备联系人恢复、新消息解密、跨平台通话、朋友权限仍需验收。没有删除历史房间、联系人数据或旧消息。

## 设备操作副作用与未完成事项

- 本次 Flutter 集成测试遗漏 `--no-uninstall`。已核实当前 Flutter 默认 `--uninstall` 为开启，测试结束调用 `adb uninstall` 且未带保留数据参数。因此当前连接的 Android 上检查包被卸载，本机应用数据也随之清除。操作前没有备份，不能承诺恢复此前的本地数据。已向用户明确说明。
- 完整应用开发包已重新构建，恢复安装连续两次返回 `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`。当时设备的已安装包列表没有 `ai.n42.www`，已请求用户在手机上允许 USB 安装。用户随后允许，完整应用 `2.4.8（2026072672）` 开发检查版现已成功安装并启动；原有本机数据未恢复。
- 后续真机测试必须使用独立测试 applicationId 或无用户数据的专用设备；对保留安装的集成测试显式传 `--no-uninstall`。不要在有未备份钱包／聊天数据的安装上运行默认的集成测试卸载流程。
