# 2026-07-03 T17 Feature Device Verification

## Scope

Task T17 on `fix/competitor-report-audit`.

Baseline:

- Branch: `fix/competitor-report-audit`
- Verified commit: `9492736a docs: 外部依赖配置总览 EXTERNAL_DEPENDENCIES.md + 两对比表指针`
- App installed on Android: `ai.n42.www` `2.4.3`, `versionCode=2026062619`

T17 priority: W1 > C1 > C3 > W4 > remaining W/C rows.

## Devices

| Device | Status | Notes |
|---|---:|---|
| Android `38f4f08a` / `25098RA98C` | INSTALLED / RUNTIME TESTED | Android 16 API 36. ADB tap/input is available on this connection. |
| macOS | BUILT | `N42 Chat.app` debug build succeeded. Runtime C-group verification needs a second logged-in chat session. |

## Build And Test Verification

| Check | Result | Notes |
|---|---:|---|
| `flutter analyze --no-fatal-infos` | PASS | One existing info remains in `packages/n42_chat/lib/src/presentation/widgets/chat/wechat_message_menu.dart`: `curly_braces_in_flow_control_structures`. |
| Targeted T17 unit tests | PASS | 187 tests passed. Covered AA/EIP-7702/UserOperation, custom chain registry, transaction replacement helpers, price alert utils, and whiteboard controller. |
| `flutter build apk --debug --target-platform android-arm64 --no-pub` | PASS | Built `build/app/outputs/flutter-apk/app-debug.apk`. |
| Android reinstall | PASS | `adb install --no-streaming -r -t -d build/app/outputs/flutter-apk/app-debug.apk` returned `Success`. |
| Android cold launch | PASS | Launcher monkey opened wallet home; foreground UI rendered normally. |
| `flutter build macos --debug --no-pub` | PASS | Built `build/macos/Build/Products/Debug/N42 Chat.app`; third-party macOS deployment/SQLCipher warnings only. |

Targeted test command:

```text
flutter test test/features/aa/eip7702_handler_test.dart test/features/aa/smart_account_test.dart test/features/aa/user_operation_test.dart test/features/wallet/custom_chain_registry_test.dart test/features/wallet/market/price_alert_sheet_utils_test.dart test/features/wallet/pages/transactions/transaction_record_helpers_test.dart packages/n42_chat/test/unit/widgets/whiteboard_controller_test.dart --no-pub
```

## Runtime Matrix

| # | Scenario | Result | Evidence / Notes |
|---|---|---:|---|
| W1 | Sepolia transaction accelerate | BLOCKED | Current Android wallet is not backed up and has no funded Sepolia pending transaction. The device still shows `You haven't backed up your wallet seed phrase!`, so no safe pending nonce/replacement window was available. |
| W2 | Sepolia transaction cancel | BLOCKED | Same blocker as W1: requires backed-up funded Sepolia wallet and a live pending transaction. |
| W3 | Custom chain send continuation | PARTIAL | ADB clipboard command is unavailable on this ROM, but `adb shell input keyboard text` reliably enters ASCII URLs. `https://rpc.gnosischain.com` failed the old add-chain gas-price check once; host curl to the same RPC succeeded. `Gnosis` and `Blast` already appear as built-in chains in this build, so they are not clean custom-chain persistence probes. A non-built-in `ModeT17/T17M/34443` entry using `https://mainnet.mode.network` submitted and returned to wallet home without crash; persistence could not be confirmed from UI or `shared_prefs` grep, so not marked full PASS. |
| W4 | AA send page real balance | PASS | Smart Account `T167702B / EIP-7702 Account / Deployed / 0xea31...25aa` opened. AA Send page showed `Available Balance: 0 ETH`; tapping `Max` on zero balance left amount empty and Send disabled. The old `1.5 ETH` mock balance was not present. |
| C1 | Realtime whiteboard co-draw | BLOCKED | Android Chat is on the welcome/login page. No two logged-in chat sessions are available. |
| C2 | No stroke message bubbles | BLOCKED | Requires C1 chat room runtime. |
| C3 | FTS5 cross-session full-text search after restart | BLOCKED | Requires at least one logged-in chat session with searchable message history; Android Chat is not logged in. |
| C4 | TTS read aloud long-press | BLOCKED | Requires logged-in chat message timeline. |
| C5 | Quiz answer reveal | BLOCKED | Requires logged-in chat room and quiz message flow. |
| C6 | Emoji panel / doodle / video note / Gemma settings | BLOCKED | Requires logged-in chat session. |

## Device Notes

- Android clipboard helper is not implemented on this ROM:
  `adb shell cmd clipboard set-text ...` and `get-text` both return `No shell command implementation`.
- `adb shell input text` remains unsafe with Chinese IMEs, but `adb shell input keyboard text ...` works for numbers and ASCII URLs.
- The old wallet `Add custom chain` entry validates by `eth_gasPrice` only. The newer `CustomChainService` / `AddCustomChainPage` exists in code but no route reference was found in this branch during T17.
- No Android app crash was observed during wallet/AA/custom-chain/chat welcome-page navigation.

## Blockers

1. W1/W2 need a backed-up wallet, Sepolia ETH, and a deliberately pending transaction. The current device wallet is unfunded and blocked by the backup guard for transfer flows.
2. C1-C6 need two logged-in chat sessions. Android is currently logged out at the `N42 Chat` welcome page; macOS build is ready but cannot complete paired chat scenarios without credentials/sessions.
3. W3 is limited by existing built-in `Gnosis`/`Blast` entries and the legacy add-chain UI not exposing a clear persisted custom-chain record for the `ModeT17` probe.
