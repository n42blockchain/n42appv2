# 2026-06-28 Wallet H1 Device Regression

## Scope

Task T11: wallet H1 feature regression for S1/S4/S5/M2 on the latest
`master` baseline.

## Status

Code/build verification: PASS.

Full device interaction regression: BLOCKED. The connected Android device
rejected debug APK installation twice with
`INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`, and the remaining
T11 scenarios require an unlocked wallet with real holdings / test assets.

## Build Verification

| Check | Result | Notes |
|---|---:|---|
| `flutter analyze --no-fatal-infos` | PASS | No issues found. |
| `flutter test test/features/wallet/eip681_test.dart test/features/wallet/scan_to_pay_utils_test.dart test/features/wallet/nft_gallery_utils_test.dart test/features/wallet/nft_batch_transfer_utils_test.dart packages/n42_chat/test/unit/widgets/panel_reaction_a11y_test.dart --no-pub` | PASS | 23 tests passed. |
| `flutter build apk --debug --target-platform android-arm64 --no-pub` | PASS | Built `build/app/outputs/flutter-apk/app-debug.apk`. |
| `flutter build ios --debug --no-codesign --no-pub` | PASS | Built `build/ios/iphoneos/Runner.app`. |

## Device Availability

- Android: `25098RA98C`, adb id `38f4f08a`, Android 16/API 36.
- iPhone: iPhone 17 Pro Max, iOS 27.0, paired wirelessly.

## Android Install Attempts

Both commands were blocked by the device:

- `flutter install -d 38f4f08a --debug`
- `adb install -r -d build/app/outputs/flutter-apk/app-debug.apk`

Result:

```text
INSTALL_FAILED_USER_RESTRICTED: Install canceled by user
```

## T11 Runtime Matrix

| Item | Runtime Result | Notes |
|---|---:|---|
| S1 stablecoin earn | NOT RUN | Needs installed app + wallet session; verify Aave V3 markets, APY sorting, TVL, refresh, and Supply navigation. |
| S4 NFT advanced management | NOT RUN | Needs wallet address with real NFTs; verify spam hide, count, show toggle, group view, and no false positive hiding. |
| S5 popular network quick-add | NOT RUN | Needs installed app + wallet session; verify 2-3 presets with `eth_chainId` and chain switching. |
| M2 receive QR to scan | NOT RUN | Needs two devices; verify EIP-681 recipient extraction and amount request handoff. |

## Next Runtime Steps

1. Allow debug install on Android or deploy through TestFlight/iOS signing.
2. Use a wallet with stablecoin holdings and at least one real NFT collection.
3. Use a second device for the QR scan scenario.
4. Capture screenshots for each S1/S4/S5/M2 row and update this report with
   PASS/FAIL evidence.

## 2026-07-01 Android Retest

See `2026-07-01-missed-install-runtime.md` for the full retest log.

| Item | Runtime Result | Notes |
|---|---:|---|
| S1 stablecoin earn | PARTIAL | Earn and Stablecoin Earn opened on device; the stablecoin page showed `No stablecoin markets available right now`, so market sorting/Supply could not be verified. |
| S4 NFT advanced management | PARTIAL | NFT Gallery opened and showed empty state; logcat reported SimpleHash unauthorized, so real NFT hide/count/group behavior could not be verified. |
| S5 popular network quick-add | PARTIAL | Ethereum network filter and custom-chain form opened; no popular quick-add preset list was observed in the tested path. |
| M2 receive QR to scan | PARTIAL/BLOCKED | Scanner opened after camera permission; Receive was blocked by seed phrase backup, and two-device QR handoff remains unavailable. |

Additional defect fixed during retest: wallet refresh was repeatedly throwing
`NoSuchMethodError` for `CoinModel.config` because `coinList` was dynamically
typed. `coinList` was narrowed to `List<CoinModel>` on the retest branch.

> 2026-07-26 correction: that narrowing was **not** carried into master. Master
> re-widened `coinList` to `List<dynamic>` to hold `AggregatedCoinModel`
> alongside `CoinModel`; the `NoSuchMethodError` is guarded by `is CoinModel`
> checks at the call sites instead. Only the retest results above were merged
> from `feat/live-gift-prediction-sync` — its code changes were dropped.
