# T23 dev513 fixes device retest - 2026-07-07

## Scope

- Worktree: `/Users/jieliu/Documents/n42/n42appv2`
- Branch: `fix/competitor-report-audit`
- Baseline at rerun: `d4fb2b9d`
- Android device: `38f4f08a`, model `25098RA98C`, Android 16
- App package: `ai.n42.www`
- Evidence screenshots: `/tmp/n42-t23-screenshots/`

## Build and automated checks

| Check | Result | Notes |
|---|---:|---|
| `flutter analyze --no-fatal-infos` | PASS | Full repo analyzer completed with no issues before device retest. |
| Targeted Flutter tests | PASS | `sp_util`, `secure_preferences`, `mining_v1`, `mining_v2`, wallet transaction detail/retry tests: 43 tests passed. |
| Android debug build | PASS | `flutter build apk --debug --target-platform android-arm64`. |
| Android install | PASS | `adb -s 38f4f08a install -r -t -d -g build/app/outputs/flutter-apk/app-debug.apk`. |

## Manual retest results

| Item | Result | Evidence / detail |
|---|---:|---|
| A1 transfer pending transaction refresh | BLOCKED | Wallet had zero ETH and gas quote failed with `error code:521`; unable to create a real pending transfer. Screenshots: `02-send-entry.png`, `03-send-eth.png`. |
| A2 BTC pending timer leak/regression | PARTIAL | BTC detail page had no transaction data and no pending BTC record, so the real pending confirmation path could not be exercised. Enter/leave/retry produced no `TxRecordItems`, `timerStartBtc`, or `checkUndoneTrBtc` log errors. Code path keeps the timer async/awaited and cancels when `_trUndoneList` is empty. Screenshots: `04-btc-detail.png`, `05-btc-detail-retry.png`. |
| A3 mining_v2 risk-card overflow at 130 percent font | PARTIAL | Font scale set to `1.3`; Earn and mining pages opened without visible overflow and logcat had no `RenderFlex overflowed`. The specific `MiningRiskCard` was hidden because this account had `depositsEnable == false`, so the exact long-language risk-card case still needs an account that can render the card. Screenshots: `08-earn-font13.png`, `09-mining-font13.png`. |
| A4 unauth mining data persistence | PASS | Added a temporary device-only probe against `SPUtil.setMiningData/getMiningData` using the unauth default key `AstranetWallet`. Write, process restart/read, and cleanup all passed. Probe was removed and the normal app was rebuilt/reinstalled afterward. Screenshots: `12-probe-write.png`, `13-probe-read.png`, `14-probe-cleanup.png`, `16-restored-main-stable.png`. |

## Notes

- The UI import-validator path was reached, but a full validator import requires encrypted validator data plus password, so the UI import path was not completed.
- Device state was restored after the probe: system font scale reset to `1.0`, temporary `lib/t23_mining_data_probe.dart` removed, normal debug APK rebuilt and installed.
- No code changes were needed for T23 during this rerun.
