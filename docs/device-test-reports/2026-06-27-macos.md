# 2026-06-27 macOS Dock Attention

## Scope

- Task: T6, macOS Dock attention build and runtime validation.
- Baseline: `master` `74f64aed` (`feat(macos): Dock attention via requestUserAttention`).
- Host: macOS 26.5.1 (25F80), Flutter 3.41.4 stable, Dart 3.11.1.

## Build Verification

| Check | Result | Notes |
|---|---|---|
| `flutter build macos --debug --no-pub` | FAIL, then PASS | First run failed during CocoaPods resolution because `macos/Podfile.lock` still pinned `WebRTC-SDK 137.7151.04` while `flutter_webrtc 1.4.0` requires `144.7559.01`. |
| `pod update WebRTC-SDK` | FAIL | CocoaPods CDN failed twice on `FirebaseAnalytics/12.15.0` with `HTTP2 framing layer`. |
| `cd macos && pod update --no-repo-update` | PASS | Updated macOS lockfile using local specs, including `WebRTC-SDK 144.7559.01` and Firebase `12.9.0`. |
| `flutter build macos --debug --no-pub` | PASS | Built `build/macos/Build/Products/Debug/N42 Chat.app`. Confirms `flutterViewController.engine.binaryMessenger` compiles against current FlutterMacOS. |

## Runtime Verification

| Check | Result | Notes |
|---|---|---|
| Launch macOS app with a temporary `--dart-define=N42_TEST_FLASH_WINDOW=true` trigger | BLOCKED | Temporary Dart trigger was added only for local validation and removed before committing. The app launched, but Dart failed before reaching the trigger. |
| Trigger `SystemIntegrationService.flashWindow()` while app is unfocused | BLOCKED | Runtime stopped at `Firebase.initializeApp()` with `[core/not-initialized] Firebase has not been correctly initialized.` |
| Dock icon bounces until app is focused | NOT VERIFIED | `macos/Runner/GoogleService-Info.plist` is absent, so the app cannot complete startup on macOS in this environment. |

## Status

T6 build verification is complete. The macOS native MethodChannel registration compiles, so the `binaryMessenger` API risk is cleared.
Dock-bounce runtime verification remains blocked by missing macOS Firebase configuration, which prevents the app from reaching the chat/system-integration trigger path.
