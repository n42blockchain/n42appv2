# TestFlight Chat 反馈修复与钱包集成 — 2026-09-15

钱包已固定 Chat [`def750f`](https://github.com/n42blockchain/n42_chat/commit/def750f16e553146c484d8a155e6819581f4d3c3)，主体修复为 [`c612f8b`](https://github.com/n42blockchain/n42_chat/commit/c612f8bb787bf15a097dae9550cc3e01bc6e7889)，均已推送 Chat 审计分支。正常 Git 解析源码、771 个 lib/assets 缓存镜像及[源码清单](../CHAT_SOURCE_MANIFEST_2026-09-12.json)逐一哈希一致，没有 Chat 本地路径覆盖。

| 反馈 | 本次处理 |
|---|---|
| 1.1 注册提示用户名或密码错误 | 去掉自动提交的内置邀请码和错误的“已内置／已填写”提示；按服务器返回的完整 UIA 流程处理 token/dummy 多阶段验证；保留真实注册错误，不再套用登录密码错误。 |
| 1.2 好友关系不对等、未同意即可发送 | 双方明确加入私聊后才成为联系人；缺少成员状态不能沿用 SDK 默认 join；未接受前限制文本、媒体、回复及转发，不提前共享朋友圈。 |
| 1.2 / 1.3 退出账号重新登录后历史消息已加密 | 按用户、设备、服务器及客户端隔离时间线／消息缓存，拒绝旧异步请求回写；使用 SDK 创建实际服务器密钥备份并保留现有秘密；恢复后核对会话密钥，失败不再报成功。加密气泡提供恢复入口，登出前提供备份提醒和入口。 |
| 1.4 下载操作残留在钱包首页 | Chat 使用独立 ScaffoldMessenger；真实页面测试覆盖点击文件、返回后提示消失，以及父页面通知仍可使用。 |
| 1.5 设置个人资料／聊天入口失效 | 补全个人资料默认认证路由并保留宿主回调；保留并验证当前版本已修复的聊天设置默认跳转。 |

用户补充的五张截图已从本机 Downloads 目录逐一检查。截图包含注册凭据，未复制进仓库、未用于登录。钱包首页截图底部的文件下载提示与 1.4 对应，设置红框与 1.5 对应。

只读查询截图所用服务器：两种大小写的合成用户名均返回可用；旧内置 token 的有效性接口返回 HTTP 403：`M_FORBIDDEN: Server does not allow token registration`。这一结果支持邀请码策略不匹配的判断；本轮没有创建账号，不能替代真实注册验收。

## 验证结果与精确版本

- Chat 主体修复 `c612f8b`：完整测试 **6,445 通过、1 跳过、0 失败**，分析 **0 错误、0 警告、173 info**。
- 钱包在该依赖下完整测试 **4,870 通过、0 失败、0 跳过**，覆盖率 **60,085 / 130,740 = 45.96%**，分析 **0 错误、0 警告、155 info**。之前完成的浏览器覆盖率修复包含在此次钱包工作区验证内。
- 随后的 `def750f` 仅增加一行已完成时间线标记的清理，插件 14 项消息仓库回归通过；钱包更新最终 pin 后重跑全部 **111 项新增接入的 Chat 回归，均通过**。不把前一版本的全量运行称为最终 pin 的另一次全量运行。
- 新回归在修复前 `0894354` 上复现 **13 项失败**（同批 22 项通过）；另有 **3 项时间线会话回归全部失败**。最终均通过。
- 九个插件测试文件接入钱包主测试入口，新增 `bloc_test` 开发依赖。26 种语言的退出备份提示均已提供并重新生成本地化；英／中／阿语 320px 加密气泡交互通过。

[机器统计及证据哈希](summary.json)。原始完整覆盖率没有排除生成代码，**仍低于正式发布的 70% 门槛**。

## TestFlight 与验收边界

本报告是构建前的源码验证记录，不能作为上传或 Apple 处理完成的凭据。IPA 构建号、源码提交、签名校验和上传结果以另行保存的本次 TestFlight 发布记录为准。

需在 iOS / Android 专用账号上验证：正常注册；互加好友的接受／拒绝与双侧列表；加密新消息；备份密钥并保存恢复密钥；退出账号重新登录并恢复历史；下载文件返回；设置路由。此前未备份且已随登出清除的历史密钥，不能由显示或缓存修复凭空恢复。注册页可选邮箱尚未完成验证绑定，详见 AUTH-001。

未决事项统一维护在 [Chat OPEN_ISSUES.md 的 QA-009 / AUTH-001](https://github.com/n42blockchain/n42_chat/blob/fix/chat-entry-audit-20260912/OPEN_ISSUES.md)。本次 TestFlight 是反馈复测包，不代表双端正式发布批准。
