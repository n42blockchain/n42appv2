# Release 2.4.8 (2026072823)

- Date: 2026-09-25.
- Source commit: `1ecf019fa937977b70bdb448a2806f6ec2c21b5b`.
- Dependency reference: `pubspec.yaml` pins `n42_chat` to `3cc19c12a7c9bbf2031270acca8e3922730b55a5`. This Mac resolved it through the local `packages/n42_chat` path override; the mirror changes have not yet been forward-ported to that Git source. This TestFlight build includes the local mirror. Sync the package source and lockfile before producing a release from a clean checkout.
- Included changes: wallet and Chat QR payment routing, strict asset resolution, UTF-8 memo gas estimation, wallet coverage interaction fixes, and removal of stale iOS scanner linker references.
- Validation: changed Dart files formatted; focused analyzer reported no issues. Automated tests were not run.
- TestFlight: Xcode export/upload exited 0 and reported `Upload succeeded`, `Uploaded Runner`, and `EXPORT SUCCEEDED`. Apple processing and tester availability were not independently checked.
- Android build used the installed JDK 21 through a local Gradle JVM override because the committed Android Studio JBR path is absent on this Mac.

## Artifact validation

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| IPA | 162884077 | `b7e2814ff8c061f6a28a3dd82959c52c7dbf9d4882fb8d0c29a74eb3286728c2` |
| APK | 847880105 | `25d97c84de9249bce0d2a45c45af807cea7d5527f7de93db10a1e446254097ec` |

The IPA main app (`ai.n42.www`) and notification extension (`ai.n42.www.NotificationExtension`) both report version 2.4.8 / build 2026072823. Deep strict code-signature verification passed for the IPA. Both bundles use team `CFRXH38L48`; the main app has production APNs and the expected app/keychain groups, and `get-task-allow` is disabled for both bundles.

The APK package is `ai.n42.www`, version 2.4.8 / build 2026072823. Android v2 signature verification passed with certificate SHA-256 `caceef05f3323baef7e080aadcb994118dec86c8cdf620ee80793b3622ea109d`. The APK and `.sha256` sidecar are in `~/Downloads/N42Wallet-2.4.8-2026072823-release.apk`.

The IPA is at `build/ios/ipa/N42Wallet.ipa` in the local build output.
