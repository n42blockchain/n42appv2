# Release 2.4.8 (2026072813)

- Date: 2026-09-24.
- Source commit: `c907476bd8c525f71f958ee8a482bccf9f423139`.
- Dependency reference: `pubspec.lock` from the source commit; `n42_chat` is pinned to `3cc19c12a7c9bbf2031270acca8e3922730b55a5` in `pubspec.yaml` and resolved through the configured local path override.
- Included commits: wallet and group chat flow improvements, XRP reserve handling for max send, and restored iPad support in iOS release builds.
- TestFlight: Xcode export/upload exited 0 and reported `Upload succeeded`, `Uploaded Runner`, and `EXPORT SUCCEEDED`. Apple reported that package processing started; tester availability is not yet confirmed.

## Artifact validation

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| IPA | 162893445 | `2b7cb8ecd83f9d11f139383411b96330e289b7e3ec8db7aa8c96ffd830559090` |
| APK | 847890017 | `a012e66ee5adf3d0f8f95d78b3df9d64756c3e856a641f9fb9717a40b7cfcd0b` |

The IPA main app (`ai.n42.www`) and notification extension (`ai.n42.www.NotificationExtension`) both report version 2.4.8 / build 2026072813 and target device families `[1, 2]`. Deep strict code-signature verification passed for the IPA. Both bundles use the expected iPhone Distribution identity and team `CFRXH38L48`; the main app has production APNs, the expected app/keychain groups, and `get-task-allow=false`. The extension also has `get-task-allow=false`.

The APK package is `ai.n42.www`, version 2.4.8 / build 2026072813. Android v2 signature verification passed with certificate SHA-256 `caceef05f3323baef7e080aadcb994118dec86c8cdf620ee80793b3622ea109d`. The file is available at `~/Downloads/N42Wallet-2.4.8-2026072813-release.apk` with a `.sha256` sidecar.

Xcode reported missing dSYMs for WebRTC and flutter_vodozemac. This did not block upload; symbolication for those frameworks may be limited. The IPA is at `build/ios/ipa/N42Wallet.ipa` in the local build output.
