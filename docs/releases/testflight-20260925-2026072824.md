# TestFlight release — 2026-09-25, build 2026072824

- App version: **2.4.8 (2026072824)**, `ai.n42.www`.
- Source commit: `9371b823e4f9cc72b9e7737fb6ed738530b7519c` (`master`). The
  dependency-upgrade worktree was paused and was not included in this build.
- Dependency source: `pubspec.yaml` declares Chat SHA
  `3cc19c12a7c9bbf2031270acca8e3922730b55a5`; the selected `master` lock resolves
  Chat from the checked-in local `packages/n42_chat` path override, matching the
  prior 2026072823 release. This record does not claim Git-source Chat validation.
- Build: `bash scripts/prepare_ios_release.sh --dart-define-from-file <local-private-config>`
  in the visible macOS Terminal session, using Xcode 27.0 / iOS SDK 27.0 and
  Flutter 3.44.8 / Dart 3.12.2. The builder preserved the committed build
  number. One initial attempt stopped because the new worktree lacked the
  ignored local `GoogleService-Info.plist`; the existing local file was copied
  into the ignored worktree path, and the second archive/export succeeded.
- TestFlight upload: `xcodebuild -exportArchive` exited `0`; Xcode reported
  `Upload succeeded`, `Uploaded Runner`, and `EXPORT SUCCEEDED`. App Store
  Connect reported that the package is processing. Processing completion and
  tester availability have not been confirmed.
- Automated test suites were not run for this release build. This is a TestFlight
  upload record, not full release acceptance.

## Artifact validation

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| IPA | 162,885,986 | `3cc0d1e0ef696a2001c7b97979e1f9e36a676af3873079a4f0509a53af47ebe0` |

The IPA is at `build/ios/ipa/N42Wallet.ipa` in the local release worktree. Deep,
strict code-signature verification passed. The app and notification extension
both contain version 2.4.8 / build 2026072824, use Team `CFRXH38L48`, target
iPhone and iPad, and have `get-task-allow=false`. The main app contains the
production APNs entitlement, `group.ai.n42.wallet`, and the expected app and iOS
keychain groups. The extension has no app-group or push entitlement, matching
its checked-in source entitlement file.

Xcode reported missing dSYMs for `WebRTC.framework` and
`flutter_vodozemac.framework`. Upload still succeeded; symbolication for those
frameworks may be limited. Machine-readable checks are in
[validation.json](testflight-20260925-2026072824/validation.json), and the
sanitized upload result is in
[upload-result.txt](testflight-20260925-2026072824/upload-result.txt).

The documentation commit advances the next development build number to
2026072825 through the repository pre-commit hook; the uploaded artifact remains
2026072824.
