# TestFlight 2.4.8 (2026072778)

## Release identity

- Source commit: `7a493b6425588dee81339ea2e84f0cdb84e4f7c9`.
- Chat dependency: `6606eed4` (current-device outbound-key policy correction).
- Main app and notification extension: 2.4.8 (2026072778).
- IPA bytes: 162922659.
- IPA SHA-256: `63f7c84e8a02b6c8258e087f219263f83ec1cab28b36b35aa3671911c75ed73b`.
- Xcode 27.0; CocoaPods 1.17.0.

## Validation and upload

- Desktop signing probe passed.
- Archive/export completed successfully.
- Both bundle versions and identifiers verified.
- Deep strict code-signature verification passed.
- Distribution team, production APNs, app/keychain groups and disabled
  development debugging entitlement verified against provisioning profiles.
- Xcode upload exited 0 and reported `Uploaded Runner` and `EXPORT SUCCEEDED`.
- Upload accepted on 2026-09-21 (America/Toronto). Apple processing completion
  and tester availability have not been verified.
- Non-blocking missing dSYM warnings: WebRTC.framework, flutter_vodozemac.framework.

## Build preparation

The local CocoaPods index initially lacked locked WebRTC-SDK 150.7871.01.
Index refresh encountered HTTP/2 CDN errors; fetching the affected official
TrustWalletCore spec files over HTTP/1.1 restored resolution. `pod install`
completed; only the Flutter pod checksum and CocoaPods metadata changed in
`ios/Podfile.lock`. No pod version was changed for this release.

## Scope and limits

Includes the encrypted-send guard correction validated by 17 targeted tests.
It prevents recipient cross-signing policy from rejecting the current sender
device, while retaining key validity and remote-device requirements.
Actual on-device encrypted-send recovery and historical-message recovery
remain unconfirmed; upload success does not establish either.

The documentation commit advances the next development build automatically;
it does not alter this uploaded artifact.
