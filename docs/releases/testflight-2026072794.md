# Release 2.4.8 (2026072794)

- Date: 2026-09-22.
- Source: `29aea0960f5511b5584efabd92e89941f86a04d5`.
- Chat dependency reference: `3cc19c12a7c9bbf2031270acca8e3922730b55a5`; the app build used the committed local mirror through `pubspec_overrides.yaml`.
- TestFlight: Xcode upload exited 0 and reported `Upload succeeded`, `Uploaded Runner`, and `EXPORT SUCCEEDED`. Apple processing started; tester availability is not yet confirmed.

## Included change and validation

When this device shares its screen, the call view no longer renders a shared video track locally. Android screen capture includes the call view itself; rendering the local shared track there fed the capture back into its own video, producing recursively nested screen images. Remote screen sharing continues to render normally.

Focused widget tests passed: `flutter test --no-pub --reporter expanded test/unit/widgets/group_call_screen_test.dart` (5 tests). Same-device regression still needs confirmation on a physical device.

## Artifact validation

The IPA's main app and notification extension both report version 2.4.8 / 2026072794. Deep strict signature verification passed. App Store Store provisioning profiles, distribution team, bundle identifiers, production push entitlement, main app group, and `get-task-allow=false` were verified.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| IPA | 162929197 | `14547ea31448c1a9fe1ab5e8909b0d29dde0d4d75b224e537ace46ec9e7d0c64` |

Xcode reported missing dSYMs for WebRTC and flutter_vodozemac. Upload succeeded; these warnings limit symbolication for those frameworks.
