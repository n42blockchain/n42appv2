// AAR 模块桥接：将 evm.aar 作为 Gradle 子项目暴露，
// 使 flutter_mining library 能通过 project(':evm-module') 依赖它，
// 避免 "Direct local .aar file dependencies are not supported" 错误。
configurations.maybeCreate("default")
artifacts.add("default", file('../libs/evm.aar'))
