org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+UseSerialGC -XX:+HeapDumpOnOutOfMemoryError -XX:TieredStopAtLevel=1
android.useAndroidX=true
android.enableJetifier=true
org.gradle.daemon=false

# Use Eclipse Temurin JDK 21
#org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.9.10-hotspot
# LOCAL TRIAL: openjdk@21 21.0.10 (Homebrew) crashes deterministically
# (SIGSEGV at the same address) mid Rust cross-compile on this machine/macOS
# build. openjdk@17 avoids the crash but is source-incompatible with this
# project's Java 21 target. Trying Android Studio's bundled JBR 21 instead
# (same major version, different vendor build). Not meant to be committed.
org.gradle.java.home=/Applications/Android Studio.app/Contents/jbr/Contents/Home
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
